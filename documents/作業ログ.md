# 福元鍼灸整骨院 予約システム 作業ログ

## プロジェクト概要
- **ドメイン**: kojinius.jp
- **Firebase**: Online Appointment System
- **構成**: Firebase Hosting + Firestore + Functions + Authentication

---

## 2026-03-05 作業記録

### 完了した作業

#### ドメイン取得
- さくらインターネットで `kojinius.jp` を取得
- 支払い完了（クレジットカード）

#### Firebase環境構築
- Firebaseプロジェクト作成（Online Appointment System）
- Firestore DB作成
  - プラン: Standard Edition
  - リージョン: asia-northeast1（東京）
  - セキュリティルール: 厳密
- Firebase Authentication設定（メール/パスワード有効化）
- Firebase Hosting設定

#### Firebase CLI設定
- `firebase-tools` グローバルインストール
- `firebase login` 完了
- `firebase init`（Firestore + Functions + Hosting）
  - Functions言語: JavaScript
  - Publicディレクトリ: `.`（ルート）
  - SPA設定: No
- functions の node_modules はGoogleドライブ制約のため
  `C:\Users\SM7B\Workspace\dev\Fukumoto` に配置
- `firebase.json` の `functions.source` を上記パスに変更

#### DNS・SSL設定
- Firebaseコンソールにカスタムドメインとして kojinius.jp を登録
- さくらのDNSゾーン設定に以下を追加:
  - Aレコード: `@` → `199.36.158.100`
  - TXTレコード: kojinius.jp → hosting-site-project-...（Firebase検証用）
- SSL証明書はDNS反映後にFirebaseが自動発行

---

## 2026-03-06 作業記録

### 完了した作業

#### DNS・SSL確認
- `nslookup kojinius.jp` → `199.36.158.100` 確認済み ✅
- Firebaseコンソールでカスタムドメイン「接続されています」確認済み ✅

#### Resendメール送信API構築
- Resendアカウント作成・APIキー発行（`fukumoto-reservation`）
- APIキーはFirebase Secret Manager（`RESEND_API_KEY`）で管理
- さくらDNSにResend用レコード追加:
  - TXT `resend._domainkey` → DKIM
  - MX `send` → `10 feedback-smtp.ap-northeast-1.amazonses.com.`（さくらはMXに優先度を含める形式）
  - TXT `send` → `v=spf1 include:amazonses.com ~all`
- Resendダッシュボードで `kojinius.jp` Verified ✅
- Firebase Functions `sendReservationEmail` 実装・デプロイ完了
  - エンドポイント: `https://sendreservationemail-po3aztuimq-uc.a.run.app`
  - テスト送信成功 ✅
  - curlでテスト → `{"success":true,"id":"7a7fae47-99ff-46bb-badb-03cce09b32ee"}` 確認
  - Gmail受信確認済み（迷惑メールフォルダ、文字化けなし）

#### GCP権限設定（トラブルシューティング）
- Cloud Build SA に `Secret Manager のシークレット アクセサー` 付与
- `214213328195-compute@developer.gserviceaccount.com` に `ログ書き込み`・`編集者` 付与
- **TODO（リリース前）**: `編集者` ロールを最小権限に絞り直す

---

## 次回作業予定

1. Phase 2：localStorage → Firestore 移行

---

## 備考
- さくらのレンタルサーバー契約は不要（Firebase Hostingで代替）
- さくらDNSのMXレコードはデータ欄に `優先度 + 半角スペース + ホスト名.` の形式で入力する
