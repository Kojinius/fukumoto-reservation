# 福元鍼灸整骨院 予約システム 作業ログ

## プロジェクト概要
- **ドメイン**: kojinius.jp
- **Firebase**: Online Appointment System
- **構成**: Firebase Hosting + Firestore + Functions + Authentication

---

## 2026-03-05 作業記録

### 完了した作業

#### ドメイン取得
- さくらインターネットで `kojinius.jp` を取得
- 支払い完了（クレジットカード）

#### Firebase環境構築
- Firebaseプロジェクト作成（Online Appointment System）
- Firestore DB作成
  - プラン: Standard Edition
  - リージョン: asia-northeast1（東京）
  - セキュリティルール: 厳密
- Firebase Authentication設定（メール/パスワード有効化）
- Firebase Hosting設定

#### Firebase CLI設定
- `firebase-tools` グローバルインストール
- `firebase login` 完了
- `firebase init`（Firestore + Functions + Hosting）
  - Functions言語: JavaScript
  - Publicディレクトリ: `.`（ルート）
  - SPA設定: No
- functions の node_modules はGoogleドライブ制約のため
  `C:\Users\SM7B\Workspace\dev\Fukumoto` に配置
- `firebase.json` の `functions.source` を上記パスに変更

#### DNS・SSL設定
- Firebaseコンソールにカスタムドメインとして kojinius.jp を登録
- さくらのDNSゾーン設定に以下を追加:
  - Aレコード: `@` → `199.36.158.100`
  - TXTレコード: kojinius.jp → hosting-site-project-...（Firebase検証用）
- SSL証明書はDNS反映後にFirebaseが自動発行

---

## 2026-03-06 作業記録

### 完了した作業

#### DNS・SSL確認
- `nslookup kojinius.jp` → `199.36.158.100` 確認済み ✅
- Firebaseコンソールでカスタムドメイン「接続されています」確認済み ✅

#### Resendメール送信API構築
- Resendアカウント作成・APIキー発行（`fukumoto-reservation`）
- APIキーはFirebase Secret Manager（`RESEND_API_KEY`）で管理
- さくらDNSにResend用レコード追加:
  - TXT `resend._domainkey` → DKIM
  - MX `send` → `10 feedback-smtp.ap-northeast-1.amazonses.com.`（さくらはMXに優先度を含める形式）
  - TXT `send` → `v=spf1 include:amazonses.com ~all`
- Resendダッシュボードで `kojinius.jp` Verified ✅
- Firebase Functions `sendReservationEmail` 実装・デプロイ完了
  - エンドポイント: `https://sendreservationemail-po3aztuimq-uc.a.run.app`
  - テスト送信成功 ✅
  - curlでテスト → `{"success":true,"id":"7a7fae47-99ff-46bb-badb-03cce09b32ee"}` 確認
  - Gmail受信確認済み（迷惑メールフォルダ、文字化けなし）

#### GCP権限設定（トラブルシューティング）
- Cloud Build SA に `Secret Manager のシークレット アクセサー` 付与
- `214213328195-compute@developer.gserviceaccount.com` に `ログ書き込み`・`編集者` 付与
- **TODO（リリース前）**: `編集者` ロールを最小権限に絞り直す

---

---

## 2026-03-06 作業記録（続き）

### 完了した作業

#### ディレクトリ構成整理
- `style.css` → `css/style.css` に移動
- `app.js` / `admin.js` → `js/` フォルダに移動（`js/config.js`, `js/firebase.js`, `js/auth.js`, `js/utils.js` も新規作成）
- `dev/Fukumoto`（Functionsの回避策フォルダ）を廃止し、`functions/` に統合
- `firebase.json` の `functions.source` を `./functions` に修正
- `firebase.json` の `hosting.ignore` に `functions/**`, `README.md`, `firestore.rules` 等を追加（本番非公開化）

#### Workspace移行
- `G:\マイドライブ\Workspace` → `C:\Users\SM7B\Workspace` に統合移行
- GドライブはGoogleDriveSync経由のバックアップとして残存
- Windows Terminal 起動フォルダを `C:\Users\SM7B\Workspace` に変更

#### Phase 2 実装（localStorage → Firestore 移行）
- **js/config.js**（新規）: Firebase設定値モジュール
- **js/firebase.js**（新規）: Firebase初期化・db/auth エクスポート
- **js/auth.js**（新規）: requireAdmin / login / logout（admin Custom Claim チェック）
- **js/utils.js**（更新）: ES Module export 追加（esc / DAY_NAMES / formatDate 等）
- **js/app.js**（全面書き換え）: Firestoreスロット取得・runTransactionによる二重予約防止
- **js/admin.js**（全面書き換え）: requireAdmin認証ガード・onSnapshotリアルタイム同期・writeBatch更新
- **login.html**（新規）: スタッフ専用ログイン画面
- **firestore.rules**（更新）: デフォルト拒否・isValidReservationバリデーション
- **functions/index.js**（更新）: setInitialAdmin Function 追加
- **index.html / admin.html**（更新）: type="module" 対応・ログアウトボタン追加
- **README.md**（更新）: 技術スタック・ディレクトリ構成を最新化
- コミット: `50e765e` "feat: Phase 2 実装 — Firestore移行・認証・二重予約防止・リアルタイム同期"
- GitHub (main) プッシュ済み ✅

---

---

## 2026-03-06 作業記録（続き3）

### 完了した作業

#### Firebase Emulator 環境構築
- Java 25 LTS インストール
- `firebase init emulators`（Authentication / Functions / Firestore / Hosting）
- `js/firebase.js` をエミュレーター対応に修正（`connectFirestoreEmulator` / `connectAuthEmulator`）
- `firebase emulators:start` で全エミュレーター起動確認 ✅

#### 管理者初期設定（adminクレーム付与）
- Google Cloud CLI インストール・`gcloud auth application-default login` 完了
- `gcloud auth application-default set-quota-project` でquotaプロジェクト設定
- Firebase Auth（本番）にボスのメールアドレスでアカウント作成
- Node.jsスクリプトで本番アカウントに `admin: true` クレーム付与 ✅
- Emulator上のユーザーにも `admin: true` クレーム付与 ✅
- `http://localhost:5000/login.html` でログイン・管理画面アクセス確認 ✅
- `setInitialAdmin` Function を `--only functions:sendReservationEmail` で再デプロイし削除 ✅

---

## 2026-03-07 作業記録

### 完了した作業

#### admin@kojinius.jp メール設定（Google Workspace）
- Google Workspace Business Starter（14日無料試用）契約
- `kojinius.jp` のドメイン所有権確認：さくらDNSにTXTレコード追加
  - TXT `@` → `google-site-verification=...`
- Gmail有効化：さくらDNSにMXレコード追加
  - MX `@` → `1 SMTP.GOOGLE.COM.`
- 送受信テスト完了 ✅

#### Phase 3 実装（通知機能）
- **sendReservationEmail**（更新）: 予約番号・キャンセル案内をメールテンプレートに追加 ✅
- **notifyAdminOnReservation**（新規）: 予約確定時に `admin@kojinius.jp` へ HTTP Function で通知 ✅
  - 当初 Firestoreトリガー（onDocumentCreated）で実装したが Eventarc 権限問題で発火せず → HTTP Function に変更
- **sendDailyReminders**（新規）: 毎日15:00 JST に翌日の予約者へリマインダー自動送信 ✅
  - Cloud Scheduler で `firebase-schedule-sendDailyReminders-us-central1` ジョブ自動作成済み
- **firestore.indexes.json**（更新）: `date + status` の複合インデックス追加
- **js/app.js**（更新）: 予約確定時に管理者通知エンドポイントを非同期呼び出し追加
- コミット: `2ffcdb4` "feat: Phase 3 実装 — 患者確認メール強化・管理者新着通知・前日リマインダー"
- GitHub (main) プッシュ済み ✅

---

---

## 2026-03-07 作業記録（続き2）

### 完了した作業

#### Phase 4 仕上げ（4-3）PDF日本語フォント対応
- `NotoSansJP-Regular.woff2`（日本語）・`NotoSansJP-Latin.woff2` を `fonts/` に配置
- `js/app.js` の `exportPdf()` を fontkit 対応に全面書き換え
  - `pdfDoc.registerFontkit(fontkit)` でフォント登録
  - フォントをメモリキャッシュ（`_fontCache`）して2回目以降の出力を高速化
  - PDFラベルを日本語化（氏名・ふりがな・生年月日 等）
- 電話番号・住所のプレースホルダー修正は Phase 6 管理画面エンハンスに移管

---

#### Phase 4 仕上げ（4-1・4-2）
- **4-1 個人情報同意チェックボックス追加**
  - `index.html` Step2フォームに「個人情報の取り扱い」セクションを追加
  - カスタムチェックボックス（`#consentCheck`）を実装
  - `js/app.js` の `validateForm()` に同意チェックのバリデーション追加
  - `css/style.css` に `.consent-label` / `.consent-check-box` スタイル追加
- **4-2 プライバシーポリシーページ作成**
  - `privacy-policy.html` 新規作成（10項目のポリシー掲載）
  - 同意チェックのリンク先として連携済み

---

## 2026-03-07 作業記録（続き3）

### 完了した作業

#### Phase 6 管理画面エンハンス — 営業時間設定（1-5）拡張

- **午前・午後それぞれのON/OFF対応**
  - `renderBizHoursTable()` に「午前」「午後」チェックボックスを追加（10列構成）
  - `toggleBizAmPm(d, period)` 追加：チェックOFFで対応selectをdisabled化
  - `getBizHoursFromForm()` に `amOpen` / `pmOpen` を含めて保存
  - `DEFAULT_BIZ_HOURS` に `amOpen: true, pmOpen: true` 追加済み
- **営業時間プルダウンの範囲制限**
  - 午前：7:00〜12:00、午後：12:30〜22:00 に分離
  - `genTimeOptions(fromH, fromM, toH, toM)` に引数追加、`AM_TIMES` / `PM_TIMES` 定数化
- **曜日チェックOFF → 休日カレンダー連動**
  - `getWeekdayOpenStateFromForm()` 追加（フォームのチェック状態を取得）
  - `renderHolidayCal()` 更新：休診曜日を `.weekday-closed`（グレー）で表示、tooltip表示
  - `toggleBizDay()` から `renderHolidayCal()` を呼び出し即時反映
  - `css/style.css` に `.cal-cell.weekday-closed` スタイル追加
- **営業時間バリデーション**
  - `validateBizHours()` 追加：開始≥終了エラー・営業日なのに午前午後ともOFFのエラー
  - `saveSettings()` の保存前に呼び出し、エラー時はFirestore保存をキャンセル
- **`app.js` `buildBusinessHours()` 更新**：`amOpen` / `pmOpen` を参照して午前・午後スロットを動的生成

#### 2-4 性別欄の追加（患者側）

- `index.html` Step2「基本情報」に性別ラジオを追加（男性・女性・回答しない、任意・デフォルト未選択）
- `index.html` Step3確認画面に性別行を追加
- `js/app.js`：booking オブジェクト・`fillConfirmation()`・`newReservation()` リセットに性別を反映
- `js/admin.js`：詳細モーダル・CSV出力に性別列を追加

#### 2-2 予約キャンセル機能（患者側）

- **`cancel.html`**（新規）：予約番号＋電話番号で検索 → 詳細確認 → キャンセル実行 → 完了
- **`js/cancel.js`**（新規）：
  - Firestore から1件取得、電話番号照合（クライアント側）
  - `cancelCutoffMinutes` 設定を参照してキャンセル期限チェック
  - `writeBatch` で `reservations` + `slots` を一括キャンセル更新
- **`firestore.rules`** 更新：
  - `reservations`：患者による `get`（1件読み取り）と `status: cancelled` への限定 `update` を追加
  - `slots`：`cancelled → pending`（再予約）と `* → cancelled`（キャンセル）の双方向 update を許可
- `index.html` Step4 完了画面にキャンセルページへのリンクを追加

#### バグ修正・改善

- **2回目予約でエラー（403）**：キャンセル済みスロットへの `tx.set()` が `update` 扱いとなりルール違反 → `slots` の `cancelled → pending` update を許可するルールを追加
- **予約番号8桁化**：Firestore自動IDから8桁英数字（紛らわしい文字除外）に変更（`generateBookingId()`）
- **管理画面詳細モーダルのステータス日本語化**：`pending → 未確認`、`confirmed → 確認済み`、`cancelled → キャンセル`

---

## 2026-03-08 作業記録

### 完了した作業

#### 院名・電話番号・住所の全ページ動的反映

- 全ページ（index.html / admin.html / cancel.html / login.html / privacy-policy.html）に `id="clinic-name-heading"` を付与
- `js/app.js` の `loadClinicSettings()` で Firestore の `clinicName` / `phone` を読み込み、ヘッダー・タイトル・Step4 電話番号 に即時反映
- `js/cancel.js`・`login.html` にも同様の反映処理を追加

#### 管理画面設定の拡張（院内情報タブ）

- **HP URL** フィールド追加（`settingClinicUrl`）・`new URL()` による形式バリデーション
- **郵便番号** フィールド追加（`settingClinicZip`）・zipcloud API による住所自動取得ボタン
- **院の住所** フィールド追加（`settingClinicAddress`）
- 院名・電話番号を必須項目化（未入力時は保存をキャンセル）
- 保存後に院名をヘッダー・タイトルに即時反映

#### 予約完了メール強化（Functions）

- `functions/index.js` の `sendReservationEmail`：住所・電話番号・URL・Google マップリンクボタンをメール末尾に追加
- 全 Function で `getClinicSettings()` により clinicName・phone を Firestore から動的取得

#### 患者フォーム — 住所フィールド3分割

- `index.html` Step2 の住所入力を「郵便番号（必須）」「都道府県・市区町村・番地（必須）」「アパート・マンション名（任意）」の3フィールドに変更
- `js/app.js` に `fetchAddressFromZip()` 追加（zipcloud API 呼び出し → `addressMain` に自動入力）
- 確認画面（Step3）に郵便番号行追加（`〒` プレフィックス付き）
- booking オブジェクト：`zip` を追加、`address` を `addressMain + addressSub` の結合に変更

#### Google Maps 埋め込み（予約完了画面）

- `index.html` Step4 に Google Maps iframe セクションを追加（`id="clinic-map-section"`）
- `loadClinicSettings()` で clinicAddress が設定されている場合に iframe を動的注入（`&hl=ja` で日本語表示）

#### PDF 強化

- 住所の折り返し：14文字×3行（最大42文字）対応
- フッターに郵便番号行・電話番号・営業時間テキスト（`buildBizHoursText()` で動的生成）を追加

#### カレンダー表示バグ修正

- **`js/app.js` `renderCalendar()`**：`isDisabled` 判定から `dow === 0`（日曜ハードコード）を削除
  - 日曜の open/closed は `BUSINESS_HOURS[0]` の有無で判定されるように変更
- **`loadClinicSettings()`**：`buildBusinessHours()` の結果が空の場合（全曜日 `open: false`）はデフォルトの `BUSINESS_HOURS` を維持するフォールバックを追加
  - これにより、Firestore に全曜日 `open: false` のデータが存在しても全日グレーアウトにならない

---

## 2026-03-08 作業記録（続き）

### 完了した作業

#### カスタムロゴアップロード機能
- Cropper.js を使用した 80×80 正方形クロップUI を管理画面「院内情報」タブに追加
- クロップ後の base64 PNG を Firestore `settings/clinic.clinicLogo` に保存
- index.html / admin.html / cancel.html / login.html / privacy-policy.html のヘッダーロゴに反映

#### カラーテーマ切替機能
- `js/utils.js` に `THEMES`（6種：warm/navy/forest/rose/sky/charcoal）と `applyTheme()` を追加
- CSS カスタムプロパティ（`--brown`, `--orange` 等）をテーマに応じて上書き
- 管理画面「院内情報」タブにテーマピッカーUIを追加、全ページに反映

#### お知らせ・警告バナー機能
- 管理画面に「お知らせ」タブを追加（ON/OFF・タイプ・メッセージ・表示期間）
- タイプ：`info`（青）/ `warning`（オレンジ）/ `maintenance`（赤）
- 表示期間は `datetime-local` で開始・終了日時を指定（空欄 = 期間制限なし）
- index.html / cancel.html にバナー要素を追加、ローカル時刻で期間判定して表示
- バナー色: ページ背景（beige）との混同を防ぐため全タイプの色を調整
- バリデーション：ON時はメッセージ必須、開始＞終了の逆転エラー検知

#### PDF 院名オーバーフロー対応
- 院名文字数に応じてフォントサイズを動的調整（≤12:17px / ≤17:14px / ≤22:11px / else:9px）

#### 設定モーダルUI改善
- モーダル幅 620px → 700px に拡幅
- タブが折り返さないよう `flex-wrap: nowrap; overflow-x: auto` を適用

---

## 2026-03-08 作業記録（続き2）

### 完了した作業

#### 「電話する」ボタン PC非表示・スマホのみ表示
- `css/style.css`：`.header-phone` を基本 `display:none`、≤900px で表示＋`tel:` リンクスタイル追加
- `css/style.css`：`.btn-phone-mobile`（管理画面詳細モーダルの「📞 電話する」）同様に PC非表示・スマホのみ表示
- `js/app.js`：`header-phone` の内容を `<a href="tel:...">` リンクに変更
- `js/admin.js`：「電話する」ボタンに `.btn-phone-mobile` クラス追加

#### 本番ディレクトリ構成変更（5-5）
- 全アプリファイルを `apps/FukumotoGroup/OnlineAppointSystem/` 配下に移動
- ルート `index.html` をポートフォリオページ（「Claude で作っています」）に刷新
- `js/auth.js`：`/login.html` を相対パス `login.html` に修正
- `reset-password-done.html`：同上
- `js/app.js`：フォントパスを `/apps/FukumotoGroup/OnlineAppointSystem/fonts/...` の絶対パスに更新
- `firebase.json`：末尾スラッシュなし URL 用 rewrite 追加
- README.md：URL・ディレクトリ構成を最新化

#### Notion WBS 新規タスク追加（エンハンス要件_20260308 より）
- E1-11 スタッフ用メールアドレス・パスワード変更（メール認証付き）
- E3-3 メンテナンス期間設定機能追加（maintenance.html遷移）
- E3-4 メンテナンスメッセージUIスライドスイッチ化
- 5-4 「予約管理（スタッフ）」ボタン本番非表示対応
- 5-5 本番ディレクトリ構成変更（追加と同時に実装完了 → 完了タグ付き）

---

## 2026-03-08 作業記録（続き3）

### 完了した作業

#### E3-4 お知らせON/OFFをスライドスイッチ化
- `css/style.css`：`.toggle-switch` / `.toggle-track` / `.toggle-label` スタイル追加
- `admin.html`：`#annActive` チェックボックスを `<label class="toggle-switch">` ラッパーのスライドスイッチ型 UI に変更

#### E3-3 メンテナンス期間設定機能追加
- `admin.html`：お知らせタブに「🔧 メンテナンス期間 from/to」フィールド（`#maintStartDate` / `#maintEndDate`）を追加
- `js/admin.js`：メンテナンス期間の読み込み・保存・バリデーション（start ≥ end チェック）対応
- `js/app.js`：`checkMaintenancePeriod()` 追加。期間内なら `maintenance.html` に `location.replace()` でリダイレクト
- `js/cancel.js`：同様のメンテナンス期間チェック追加

---

## 2026-03-08 作業記録（続き4）

### 完了した作業

#### E1-11 スタッフ用メールアドレス・パスワード変更機能
- `admin.html`：設定モーダルに「アカウント設定」タブを追加
- `js/admin.js`：`EmailAuthProvider` / `reauthenticateWithCredential` / `updatePassword` / `verifyBeforeUpdateEmail` を import
- `js/admin.js`：`requireAdmin()` の戻り値を `currentUser` に格納
- `js/admin.js`：`openSettings()` でアカウントタブ初期化（現在のメールアドレス表示）
- `js/admin.js`：`saveAccount()` 追加（再認証 → メールアドレス変更確認メール送信 or パスワード変更）

#### ① Gmail プリフェッチによるリンク期限切れ対策
- `auth-action.html` 新規作成（カスタムアクションURLハンドラー）
  - `mode=resetPassword`：`verifyPasswordResetCode` でコード検証のみ → フォーム表示 → 送信時に `confirmPasswordReset`（コードはフォーム送信まで消費しない）
  - `mode=verifyEmail` / `mode=recoverEmail`：`applyActionCode` を自動実行し完了表示
  - 院名・ロゴ・テーマをFirestoreから反映
- `js/auth.js`：`sendPasswordResetEmail` に `actionCodeSettings` を追加（continueURL を `auth-action.html` に向ける）
- ⚠️ **Firebase Console での追加設定が必要**：Authentication → Templates → Password reset → Action URL を `https://kojinius.jp/apps/OnlineAppointSystem/auth-action.html` に変更

---

## 2026-03-08 作業記録（続き5）

### 完了した作業

#### 各種バグ修正
- `maintenance.html`：ループバグ修正（`announcement` 判定 → `maintenance.startDate/endDate` 判定に変更）
- `css/style.css`：スライドスイッチが文字に被るCSS修正（`input` を `display:none` に変更）
- `admin.html`：システム設定モーダル幅を700px → 820px に拡張（タブが切れない幅）
- `js/admin.js`：`verifyBeforeUpdateEmail` の `unauthorized-continue-uri` エラー修正（actionCodeSettings削除）
- `auth-action.html`：`verifyAndChangeEmail` モード対応を追加

#### 5-2 UAT仕様書作成（Notion）
- Notionに受入テスト仕様書を作成（62項目）
  - URL: https://www.notion.so/31db36007a1981a9b236ee47894e2aad
- `admin.html`：ヘッダーに「📋 テスト仕様書」ボタンを追加（Notionページへリンク）

#### E3-1 削除
- 「システム管理者用ログイン画面」はE1-11実装により不要と判断、Notion WBSで【不要・削除】タグ付け

---

## 2026-03-08 作業記録（続き6）

### 完了した作業

#### Phase 9 セキュリティ対応（全タスク完了）

**優先度：高**
- **H-1** `setInitialAdmin` Function を完全削除（Cloud Functions + ローカルソースから除去）
- **H-2** Functions CORS: `localhost:5000` を許可リストから除外、メール送信前に予約存在・メール一致を検証
- **H-3** キャンセル電話番号検証をFirestoreルールに移行（`_cancelVerify == resource.data.phone`）

**優先度：中**
- **M-1** `innerHTML` によるXSS脆弱性修正（admin.js × 3箇所、app.js、cancel.js）→ `createElement('img')` に統一
- **M-2** Firestoreルール強化：フィールド型・文字数の上限検証を追加
- **M-3** Functions メール送信に形式チェック＋Firestoreの予約照合を追加
- **M-4** CDN scripts に SRI (Subresource Integrity) を追加（pdf-lib, fontkit, Cropper.js）

**優先度：低**
- **L-1** CORS localhost 削除（H-2 で対応済み、完了タグ付け）
- **L-2** 予約IDをUUID v4に変更（`crypto.randomUUID()` 採用、122ビットエントロピー）

#### その他
- スタッフアカウント追加手順書を作成（`documents/スタッフアカウント追加手順.md`）
- 管理画面ヘッダーボタンをスマホ縦表示でアイコンのみに（`btn-label` + CSS media query）
- FOUC（フラッシュ）修正：`localStorage` テーマキャッシュ + `<head>` インラインスクリプト（全8HTMLに適用）
- Skills ライブラリ更新：CSS-Theme-Switcher（FOUC対応）、Zipcloud-Address-Autocomplete、Firebase-Admin-Claims を追加

---

## 2026-03-08 作業記録（続き7）

### 完了した作業

#### 各種バグ修正・機能追加

- **メール送信停止の修正**：Functions の Firestore 存在チェック（レース競合で 403）を削除、メール形式チェックのみ残す
- **2回目以降の予約送信失敗を修正**：`visitType`・`insurance`・`contactMethod`・`symptoms`・`notes` の `undefined` フォールバック追加（Firestore SDK が undefined を拒否する仕様に対応）
- **PDF 予約番号はみ出る修正**：UUID v4（36文字）を font size 7 の2行表示に変更
- **PDF 症状はみ出る修正**：13文字×2行表示に変更
- **メアド全角修正**：`toHankaku()` を追加し電話・メールを保存前に半角正規化
- **予約番号コピーボタン追加**：予約完了画面・キャンセル画面・管理画面の3か所に `⧉` コピーアイコンを追加
- **パスワード表示トグル追加**：login.html・admin.html・auth-action.html の全パスワード入力欄（計6か所）に目アイコンを追加

---

## 2026-03-08 作業記録（続き8）

### 完了した作業

#### スキルライブラリ更新（memory/skills/）

- **`advanced-pdf-engine/SKILL.md`**：`wrapPdfText()` セクションを追加（CJK/ASCII 文字幅推定による自動折り返し）
- **`input-validation/SKILL.md`**：`toHankaku()` セクション（§7）を追加（全角ASCII→半角正規化）
- **`password-toggle/SKILL.md`**：新規作成（目アイコン付きパスワード表示トグル + コピーボタンパターン）
- **`skills/README.md`**：#19〜23 欠番補完・#24 Password-Toggle を追記

#### PDF 症状全文表示修正（app.js）

- 症状・伝達事項を `wrapPdfText()` による自動折り返しに変更（13文字クリップ方式を廃止）

---

---

## 2026-03-09 作業記録

### 完了した作業

#### ドキュメント整備 — 実スクリーンショット版ユーザーマニュアル作成

##### Playwright スクリーンショット撮影（管理者側）

- ログイン失敗の原因調査・修正
  - メールアドレスが `admin@kojinius.jp`（`.com` ではなく `.jp`）であることを確認
  - `browserSessionPersistence` の非同期処理のため `time.sleep + URL判定` が機能しない問題を特定
  - `page.wait_for_url("**/admin.html", timeout=30000)` 方式に変更して解決
- モーダル・設定タブの pointer-events 干渉問題を解決
  - `closeModal('detailModal')` / `openSettings()` / `switchSettingsTab()` を `page.evaluate()` で JS 直接呼び出しに変更
- 管理者側スクリーンショット全8枚を正常撮影（08〜15）

##### ユーザーマニュアル PPTX 刷新（実スクリーンショット版）

- `documents/make_user_manual.py` を全面書き直し
  - 画面モックアップ → 実スクリーンショット埋め込みに変更
  - PIL（Pillow）でアスペクト比を保ちながら中央配置
  - 16:9 スライド（13.33 × 7.5 インチ）
  - 20スライド構成: カバー・目次・患者側7画面・管理者側8画面・まとめ
- 出力: `documents/OnlineAppointSystem_ユーザーマニュアル.pptx`（1.3MB）
- 撮影済みスクリーンショット: `documents/screenshots/` 配下 18枚

#### documents フォルダ整理

- サブディレクトリ構成に再編（specs/ manuals/ scripts/ ops/ screenshots/）
- `福元鍼灸整骨院_本番運用ロードマップ.pptx` → `OnlineAppointSystem_本番運用ロードマップ.pptx` に改名

#### スキルライブラリ更新（pptx-engine）

- `bullet_block()` — `##` 見出し / `・` 箇条書きを縦積みするヘルパーを追加
- `divider_slide()` — 章区切り用フルカラー背景スライドパターンを追加
- `add_image_fit()` — PIL でアスペクト比保持・中央配置する画像埋め込み関数を追加
- `screen_slide()` — 左説明ペイン＋右スクリーンショットの2カラムスライドパターンを追加

---

---

## 2026-03-10 作業記録

### 完了した作業

#### OAS カスタムドメイン化（oas.kojinius.jp）

- Firebase Hosting 新サイト `oas-kojinius` を作成
- `firebase.json` を単一→配列形式のマルチサイト設定に変更（portfolio / oas の2ターゲット）
- `.firebaserc` にターゲット設定追加
- Firebase Auth 認証済みドメインに `oas.kojinius.jp` を追加（REST API 経由）
- DNS 設定（さくらのDNS）: TXT 検証 → CNAME 差し替えの2段階フロー
- SSL 証明書取得完了・疎通確認済み ✅
- `apps/OnlineAppointSystem/js/app.js`: フォントパスを絶対→相対パスに変更（`import.meta.url` 利用）
- `apps/OnlineAppointSystem/js/auth.js`: パスワードリセット URL を絶対→相対パスに変更

#### ポートフォリオ ブラッシュアップ

- `index.html`: OAS リンクを `https://oas.kojinius.jp` に更新
- `index.html`: 履歴書・職務経歴書カードを別タブ（`target="_blank"`）で開くよう変更

#### ResumeCreator / CVCreator モバイルスクロールバグ修正

- スマホでプレビュータブが表示されない問題を修正（`display: block !important` 除去）
- スマホでページがスクロールできない問題を修正
  - 原因: `body` / `app-container` の `overflow: hidden` がスクロールをブロック
  - 対策: モバイルメディアクエリで `height: auto; overflow: auto;` に上書き

#### スクリーンショット再撮影・ドキュメント更新

- `documents/capture_screenshots.py`: oas.kojinius.jp / セレクター を最新HTMLに合わせ更新
- OAS スクリーンショット再撮影（ログイン・ダッシュボード・設定各タブ等 14枚）
- ユーザーマニュアル（`manuals/OnlineAppointSystem_ユーザーマニュアル.pptx`）を最新仕様に更新
- 詳細設計書（`specs/OnlineAppointSystem_詳細設計書.pptx`）を最新仕様に更新

---

## 次回作業予定

### 残タスク（2026-03-10 最終更新）

- [ ] **5-2 UAT**：クライアントによる受入テスト実施

---

## フェーズ変更メモ
- **4-4 祝日API連携** → Phase 6「E1-3 休日設定機能」に統合
  - 祝日は営業する可能性があるため、管理画面から任意に設定できる仕様に変更
- **4-3 電話番号・住所のプレースホルダー修正** → Phase 6 管理画面エンハンスに移管

---

## 旧・次回作業予定（参考）

1. **管理者初期設定（本番稼働前1回）**
   1. Firebase Auth でスタッフのメールアドレスでアカウント作成
   2. `functions/index.js` の `setInitialAdmin` の setupKey を変更してデプロイ
   3. `firebase functions:call setInitialAdmin --data '{"email":"...", "setupKey":"..."}'` でadminクレーム付与
   4. setInitialAdmin Function を Firebase コンソールから削除
2. Phase 4：個人情報同意チェックボックス・プライバシーポリシー・電話番号等コンテンツ修正・祝日API・**スタッフ管理画面（スタッフアカウントのCRUD・adminクレーム付与/剥奪）**
3. Phase 5：Firestoreセキュリティルールデプロイ・UAT・Firebase Hosting本番デプロイ

---

## 2026-03-06 作業記録（続き2）

### 完了した作業

#### セキュリティ対応：Firebase APIキー漏洩アラート対処
- GitHub / Google Cloud からAPIキー公開アラートを受信
- Firebase Web APIキーはクライアント公開前提の識別子であり、実害なし
- GCPコンソールでHTTPリファラー制限を追加: `https://kojinius.jp/*` のみ許可
- ローカル開発はFirebase Emulatorを使用する方針に決定

---

## リリース前チェックリスト（忘れずに！）

- [ ] GCPの `編集者` ロールを最小権限に絞り直す（`214213328195-compute@developer.gserviceaccount.com`）
- [x] `setInitialAdmin` Function を Firebase コンソールから削除する（2026-03-06 完了）
- [ ] スタッフ管理画面の実装要否を確認（adminクレーム付与/剥奪をGUIで操作）
- [ ] Firebaseコンソール「Authentication → テンプレート → パスワードのリセット」でアクションURLを `https://kojinius.jp/reset-password-done.html` に変更する

### 著作権法検証（リリース前に実施）

- [ ] **Step 1** 自作コードレビュー（`js/` / `css/` / `functions/index.js`）：出典不明なロジックの混入確認
- [ ] **Step 2** npm パッケージライセンス一括チェック：`cd functions && npx license-checker --csv > ../documents/license-report.csv`
- [ ] **Step 3** CDN経由ライブラリのライセンス確認（HTML の `<script src>` を洗い出し）
- [ ] **Step 4** フォント・アイコン・画像の商用利用可否確認
- [ ] **Step 5** 外部サービスToS遵守確認（Firebase / Resend / 祝日API）
- [ ] **Step 6** ライセンス帰属表示の整備（MIT等の Attribution 対応）

---

---

## 2026-03-11 作業記録

### 完了した作業

#### バグ修正：ユーザー管理タブで「Failed to fetch」エラー

- **原因**: `functions/index.js` の CORS 許可リストに `https://oas.kojinius.jp` が含まれていなかった
  - 許可リストは `["https://kojinius.jp"]` のみで、`oas.kojinius.jp` サブドメインからのリクエストがブロックされていた
- **修正**: `allowed` 配列に `"https://oas.kojinius.jp"` を追加
- **対象 Function**: `listUsers`（ユーザー一覧取得）・`deleteUser`・`createAdminUser` すべてに影響する共通 CORS ヘッダー関数
- Firebase Functions をデプロイして本番反映済み

---

## 2026-03-11 作業記録（2）

### 完了した作業

#### 機能追加：メンテナンス中でも管理者は患者ページを閲覧可能に

- **対象ファイル**: `apps/OnlineAppointSystem/js/app.js`
- **内容**: `checkMaintenancePeriod` を async 化し、`onAuthStateChanged` で Auth 初期化完了を待ってから admin クレームを確認
  - admin クレームを持つユーザーはメンテナンス中でも `maintenance.html` にリダイレクトしない
  - `auth.currentUser` を直接参照すると初期化前に `null` になる問題を `onAuthStateChanged` で解決
- Firebase Hosting にデプロイ済み

---

## 備考
- さくらのレンタルサーバー契約は不要（Firebase Hostingで代替）
- さくらDNSのMXレコードはデータ欄に `優先度 + 半角スペース + ホスト名.` の形式で入力する
