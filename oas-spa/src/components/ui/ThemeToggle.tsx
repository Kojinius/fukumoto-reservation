/**
 * ThemeToggle — dark mode 廃止のため非表示化。
 * コンポーネント参照を壊さないようインターフェースだけ残す。
 */

interface ThemeToggleProps {
  className?: string;
}

export function ThemeToggle(_props: ThemeToggleProps) {
  return null;
}
